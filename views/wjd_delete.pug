extends left

block content

  div.panel.panel-primary
    div.panel-heading
      h3.panel-title #{title}: #{gxjb.pline.name} (#{gxjb.f_date})
 
  p.text-danger 确定要删除该信息吗?
    
  form(method='POST' action='')
    input(type='hidden' name='wjdid' value=wjd._id )
  
    button.btn.btn-primary(type='submit') 删除