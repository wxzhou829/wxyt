extends ../layout

block content
  div.col-sm-4
      
    form(method='POST' action='')
      input.form-control(type='text'  name='name'  placeholder="用户名" value=(undefined===name ? '' : name))
      input.form-control(placeholder="密码" type="password" name="password" )
      button.btn.btn-primary.form-control(type='submit') 登录   
      
    if message
      p=message